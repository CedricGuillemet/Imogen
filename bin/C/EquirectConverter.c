#include "Imogen.h"

typedef struct EquirectConverter_t
{
	int mode;
} EquirectConverter;


int main(EquirectConverter *param, Evaluation *evaluation)
{
	if (param->mode == 0)
	{
	}
	else
	{
	}
	return EVAL_OK;
}
